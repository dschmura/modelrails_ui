<%# No chrome: this block already sits inside a panel that has its own border. %>
<%= ui :empty_state, variant: :plain,
       title: "No results",
       description: "Try a different search term." %>
