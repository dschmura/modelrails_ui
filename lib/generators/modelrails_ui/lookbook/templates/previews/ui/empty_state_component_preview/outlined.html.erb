<%# A filled card — bg-surface-raised, so it lifts off the page rather than
    disappearing into it. See docs/design-tokens.md. %>
<%= ui :empty_state, variant: :outlined,
       title: "No passkeys registered",
       description: "Add one to sign in without a password." %>
