<%# ✗ Something FAILED — that is not an empty state. It has no alert role, so a
    screen reader hears this with the same weight as "you have no projects". %>
<%= ui :empty_state, title: "Could not load projects",
       description: "The server returned an error." %>
