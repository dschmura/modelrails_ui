<%# test/fixtures/adoption_app/app/views/pages/show.html.erb %>
<%= ui :alert, tone: :info %>
<%= ui(:badge, tone: :success) %>
<%= render UI::DialogComponent.new %>
<button class="btn-primary">Save</button>
<%# ui :qr_code — this is a COMMENT and must NOT count as adoption %>
